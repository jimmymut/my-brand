VITE_API_BASE_URL=http://localhost:4500
# Google OAuth client id (same OAuth client as the backend's GOOGLE_CLIENT_ID).
# Leave empty to fall back to the simulated Google picker.
VITE_GOOGLE_CLIENT_ID=
